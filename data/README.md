# Shared data
Reserved for the city simulation spreadsheet, a machine-readable CSV export,
and city/country/continent identifiers. The initial MATLAB toolkit generates a
stylized city directly and does not require these data. No replication inputs
or outputs have been copied here.
